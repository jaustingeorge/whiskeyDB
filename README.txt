whiskeyDB is a program written in the C programming language that allows a userto perform queries on an SQLite Database that contains a whiskey collection.

Future updates will include the ability to insert and delete entries into the various tables in the database.
